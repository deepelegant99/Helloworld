# Helloworld

This repo lives. Hello from Codespaces.

## Permutation helper

To print every unique permutation of a word from the terminal, run:

```bash
python permutation.py Hello
```

Replace `Hello` with any other word (e.g., `python permutation.py world`). If you omit the word, it defaults to `Hello`.
